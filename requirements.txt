# Faultline runtime + dev deps. Pin loosely; tighten before final demo.

# --- Agent runtime ---------------------------------------------------------
google-adk>=0.5.0
google-cloud-aiplatform>=1.60.0
# Python MCP SDK. google-adk's McpToolset uses it but does not pin it as
# a runtime dep, so we add it explicitly here.
mcp>=1.0.0

# --- Google Cloud observability reads --------------------------------------
google-cloud-logging>=3.10.0
google-cloud-trace>=1.13.0
google-cloud-monitoring>=2.21.0
# Cloud Run admin SDK so /demo/plant + /approve can flip the live victim
# data service's REGRESSION_MODE env var. No synthetic anomaly fallback.
google-cloud-run>=0.10.0

# --- FastAPI server + streaming --------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
pydantic>=2.7.0
python-dotenv>=1.0.1
httpx>=0.27.0

# --- Victim service OpenTelemetry -> Google Cloud --------------------------
# Faultline reads metrics via google-cloud-monitoring SDK (above); the GCP
# *exporter* for monitoring is preview-only and not needed — we only export
# traces from the victim, metrics flow the other direction.
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-gcp-trace>=1.7.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0

# --- Tests -----------------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
